sed -i 's/fun attach() {/private val receiver = object : android.content.BroadcastReceiver() {\n        override fun onReceive(context: Context?, intent: Intent?) {\n            if (intent?.action == "WIDGET_ADDED_TO_GRID") {\n                refresh()\n            }\n        }\n    }\n\n    fun attach() {/g' app/src/main/java/com/example/service/WidgetsGridEditOverlayView.kt
sed -i 's/windowManager.addView(this, layoutParams)/windowManager.addView(this, layoutParams)\n            context.registerReceiver(receiver, android.content.IntentFilter("WIDGET_ADDED_TO_GRID"), Context.RECEIVER_NOT_EXPORTED)/g' app/src/main/java/com/example/service/WidgetsGridEditOverlayView.kt
sed -i 's/windowManager.removeView(this)/windowManager.removeView(this)\n            try { context.unregisterReceiver(receiver) } catch (e: Exception) {}/g' app/src/main/java/com/example/service/WidgetsGridEditOverlayView.kt
